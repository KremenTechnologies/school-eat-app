[project]
name = "school-eat-app"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "psycopg[binary]>=3.2",
    "psycopg-pool>=3.2",
    "httpx>=0.27",
    "itsdangerous>=2.2",
]

[dependency-groups]
dev = [
    "pytest>=8",
]
